<UserControl x:Class="$safeprojectname$.Windows.Preferences.SettingsControls.ListSetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:$safeprojectname$.Windows.Preferences.SettingsControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400" 
             FontSize="14" FontFamily="Consolas">
    <StackPanel>
        <TextBlock Text="{Binding FieldName, UpdateSourceTrigger=PropertyChanged, FallbackValue=st}" Padding="0"/>
        <ListBox BorderThickness="0" 
                 Margin="10 0 0 0" 
                 ItemsSource="{Binding StringValues, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:SubListSetting DataContext="{Binding}" HorizontalAlignment="Stretch"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Separator Margin="0 1"/>
    </StackPanel>
</UserControl>
